# Langfuse three-control-loops demo. Pinned; verified against Langfuse Cloud.
# Python 3.10+ required.
#
# Snapshotted 2026-08-04 from the upstream working copy at
# ~/casa/projects/lf_quick_demo, which has no git remote and no tracked files.
# This directory is therefore the canonical home for this demo, not a fork.
langfuse==4.14.1
openai==2.47.0
fastapi[standard]==0.139.2
uvicorn[standard]==0.51.0
python-dotenv==1.2.2
httpx==0.28.1
