services:
  backend-tests:
    build:
      context: ./backend
      dockerfile: Dockerfile.test
    container_name: nyc-taxi-demo-backend-tests
    environment:
      - API_BASE_URL=http://backend:8000
    depends_on:
      - backend
    profiles: ["test"]

